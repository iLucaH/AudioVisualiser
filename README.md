# AudioVisualiser
A stand alone or VST3 application to visualize audio in real time as 2D and 3D graphical animations and displays. This application uses ffmpeg to encode to video. This project uses the juce framework. The ffmpeg libraries are release mode only, so only build in release.
